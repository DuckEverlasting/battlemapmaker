/**
 * NEXT UP: make a button object, expand it into tool, layer buttons
 * 
 * - creates element, inserts itself into the DOM
 * - listens to queue events
 * - has regular listeners as well
 * - is accessible!
 * 
 * - maybe might be able to combine with modal button?
 * 
 * - new idea: new interface: Triggerable
 * - Queueable implements Triggerable
 * - win
 */
